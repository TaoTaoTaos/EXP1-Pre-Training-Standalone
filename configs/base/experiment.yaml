experiment:
  name: "xiaoxingkai_transfer"
  description: "Standalone Neural CDE transfer training for held-out Xiaoxingkai prediction."
  save_figures: true
  save_pdf_report: true

debug:
  enabled: false
  max_lakes: null
  max_windows_per_split: null
  max_epochs: null
